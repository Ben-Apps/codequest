import { AppLoadingScreen } from "@/components/ui/app-loading-screen"

export default function Loading() {
  return (
    <AppLoadingScreen
      title="Preparing character creation"
      subtitle="Loading assets • Setting up forms • Almost there"
    />
  )
}
